version: "3.9"
services:
  server:
    build: 
      context: .
      dockerfile: Dockerfile.server
    ports: 
      - 3000:3000
    depends_on:
      - manager
  manager:
    build:
      context: .
      dockerfile: Dockerfile.manager